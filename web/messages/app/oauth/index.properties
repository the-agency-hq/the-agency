# How a trip to a repository host ended, queued as a flash message by the /app/oauth routes for the page they
# redirect to. {0} is the host's name.
exchange_failed={0} did not complete the authorization. This is what a declined or an already-used authorization looks like — try again.
install_requested=Your request to install the app is waiting for an owner of that GitHub account to approve it. Its repositories appear here once they do.
installed=GitHub access updated. The repositories the app can see now are listed below.
link_failed={0} authorized the connection, but the Organization it was for no longer exists — it may have been deleted while you were away authorizing.
linked={0} is connected.
state_mismatch=That {0} response did not match a connection started from this browser, so it was ignored.
